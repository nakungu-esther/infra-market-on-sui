# Sui Infrastructure Service Discovery Platform

A comprehensive platform for discovering, integrating, and paying for Sui blockchain infrastructure services with onchain payments and usage tracking.

## 🎨 What's New - Enhanced Design

### Vibrant Sui-Inspired Color Scheme
- **Primary Color**: Rich blue/cyan (`oklch(0.55 0.22 240)`) - Sui blockchain brand colors
- **Accent Color**: Purple gradients (`oklch(0.65 0.25 270)`) - Modern, energetic feel
- **Secondary**: Light cyan tones for cards and backgrounds
- **Gradient Buttons**: Beautiful color transitions on CTAs and interactive elements
- **Enhanced Borders**: Subtle colored borders for better visual hierarchy

### Authentication Pages Now Live! 🎉
- ✅ **Login Page** (`/login`) - Fully functional with session management
- ✅ **Register Page** (`/register`) - Multi-role support (Developer, Provider, Admin)
- ✅ **Beautiful Gradients** - Sui-inspired color schemes throughout
- ✅ **Toast Notifications** - Real-time feedback using Sonner
- ✅ **Form Validation** - Client-side and server-side validation
- ✅ **Password Security** - Minimum 8 characters, confirmation matching
- ✅ **Remember Me** - Optional persistent sessions

## 🚀 Features

### ✨ Live Features
- **Service Discovery Portal** - Browse and search infrastructure services
- **Advanced Filtering** - Filter by category, type, pricing model, tokens
- **User Authentication** - Complete email/password auth with Better-Auth
- **Sui Wallet Integration** - Connect your Sui wallet
- **Responsive Design** - Works on desktop, tablet, and mobile
- **Real-time Updates** - Session management and state updates
- **Protected Routes** - Dashboard, profile, and settings pages

### 🔐 Authentication System

**Backend**: Fully integrated with Turso database
- Better-Auth for session management
- Bearer token authentication
- Email verification support
- Secure password hashing

**Frontend**: Beautiful, functional UI
- Gradient backgrounds and modern cards
- Form validation with helpful error messages
- Redirect support for protected routes
- Auto-login after registration

## 🏗️ Architecture

### This is NOT Just Frontend!

This project uses **Next.js API Routes** as the backend, which means:
- ✅ Backend API endpoints in `src/app/api/`
- ✅ Database integration (Turso SQLite)
- ✅ Authentication system (Better-Auth)
- ✅ Server-side logic and validation
- ✅ Session management and security

**See [BACKEND_ARCHITECTURE.md](./BACKEND_ARCHITECTURE.md) for detailed explanation.**

### Tech Stack

**Frontend:**
- Next.js 15 (App Router)
- React 18
- TypeScript
- Tailwind CSS
- Shadcn/UI components
- Sui dApp Kit

**Backend:**
- Next.js API Routes (serverless)
- Turso Database (SQLite)
- Drizzle ORM
- Better-Auth
- Zod validation

**Blockchain:**
- Sui JavaScript SDK
- Sui Move (smart contracts - Phase 2)

## 📦 Getting Started

### Prerequisites
- Node.js 18+ or Bun
- Git

### Installation

1. **Clone the repository**
```bash
git clone <repository-url>
cd sui-discovery-platform
```

2. **Install dependencies**
```bash
bun install
# or
npm install
```

3. **Environment variables**
The `.env` file is already configured with:
```bash
TURSO_CONNECTION_URL=<your-turso-url>
TURSO_AUTH_TOKEN=<your-turso-token>
BETTER_AUTH_SECRET=<auto-generated>
```

4. **Run the development server**
```bash
bun dev
# or
npm run dev
```

5. **Open your browser**
```
http://localhost:3000
```

## 🎯 Try It Out

1. **Visit the homepage** - See the hero section and features
2. **Create an account** - Go to `/register` and sign up
3. **Login** - Use your credentials at `/login`
4. **Browse services** - Explore infrastructure services at `/services`
5. **View details** - Click any service to see pricing and specs
6. **Connect wallet** - Use the "Connect Wallet" button in the navbar

## 📁 Project Structure

```
src/
├── app/
│   ├── api/              # Backend API routes
│   │   ├── auth/         # Authentication endpoints
│   │   ├── services/     # Service CRUD operations
│   │   └── payments/     # Payment endpoints (mock)
│   ├── login/            # Login page ✨ NEW
│   ├── register/         # Registration page ✨ NEW
│   ├── services/         # Service listing & details
│   └── page.tsx          # Homepage
├── components/
│   ├── ui/               # Shadcn UI components
│   ├── navbar.tsx        # Navigation with auth
│   ├── footer.tsx        # Site footer
│   ├── service-card.tsx  # Service display card
│   └── search-filter.tsx # Advanced filtering
├── db/
│   ├── index.ts          # Database client
│   └── schema.ts         # Database schemas
├── lib/
│   ├── auth.ts           # Auth server config
│   ├── auth-client.ts    # Auth client hooks
│   ├── api.ts            # API utilities
│   └── sui-client.ts     # Sui blockchain client
├── hooks/                # Custom React hooks
├── store/                # State management
└── types/                # TypeScript types
```

## 🎨 Color System

The new color scheme uses OKLCH color space for vibrant, perceptually uniform colors:

```css
/* Primary - Sui Blue */
--primary: oklch(0.55 0.22 240);

/* Accent - Purple */
--accent: oklch(0.65 0.25 270);

/* Secondary - Light Cyan */
--secondary: oklch(0.75 0.12 200);
```

### Where Colors Are Used:
- **Gradients**: Buttons, hero sections, cards
- **Borders**: Subtle colored borders on inputs and cards
- **Backgrounds**: Light tints for sections
- **Interactive Elements**: Hover states, focus rings
- **Brand Elements**: Logo, navigation highlights

## 🔒 Authentication Features

### User Roles
- **Developer** - Browse and purchase services
- **Provider** - List and manage services
- **Admin** - Moderate platform content

### Security Features
- Password hashing with Better-Auth
- Secure session tokens
- Bearer token authentication
- Protected API routes
- CSRF protection
- Rate limiting (planned)

## 📡 API Endpoints

### Authentication
- `POST /api/auth/sign-up` - Register new user
- `POST /api/auth/sign-in` - Login
- `POST /api/auth/sign-out` - Logout
- `GET /api/auth/session` - Get session

### Services
- `GET /api/services` - List services
- `GET /api/services/:id` - Service details
- `POST /api/services` - Create service
- `PUT /api/services/:id` - Update service
- `DELETE /api/services/:id` - Delete service

## 🚧 Roadmap

### Phase 1: Service Discovery ✅ (Current)
- [x] Frontend UI and components
- [x] Authentication system
- [x] Database integration
- [x] API route structure
- [x] Enhanced color scheme
- [ ] Connect API to database
- [ ] Provider dashboard
- [ ] Admin panel

### Phase 2: Onchain Payments (Weeks 5-7)
- [ ] Sui Move smart contracts
- [ ] Payment processing
- [ ] Multi-token support (SUI, WAL, stablecoins)
- [ ] Entitlement system

### Phase 3: Usage Tracking (Weeks 8-10)
- [ ] API gateway integration
- [ ] Quota tracking
- [ ] Usage analytics
- [ ] Rate limiting enforcement

## 🤝 Contributing

This is a project for the Sui Request for Proposal (RFP) program. Contributions are welcome!

## 📝 License

MIT License - see LICENSE file for details

## 🙋 Support

For questions about:
- **Backend architecture** - See [BACKEND_ARCHITECTURE.md](./BACKEND_ARCHITECTURE.md)
- **Authentication** - Check `/login` and `/register` pages
- **Database** - Review `src/db/schema.ts`
- **API endpoints** - See `src/app/api/` directory

---

**Built with ❤️ for the Sui ecosystem**